# apt-get install -y gnupg linuxbrew-wrapper locales nodejs zsh wget nano nodejs npm fonts-powerline && \
# locale-gen en_US.UTF-8 && \

# ADD scripts/installthemes.sh /home/devuser/installthemes.sh
# ENV TERM xterm
# ENV ZSH_THEME agnoster
# CMD ["zsh"]

# git config --global user.name "Your Name"
# git config --global user.email "youremail@domain.com"

RUN apt update && \
apt install -y git sudo curl passwd
RUN curl -O https://dl.google.com/go/go1.21.0.linux-amd64.tar.gz

RUN rm -rf /usr/local/go && \
tar -C /usr/local -xzf go1.21.0.linux-amd64.tar.gz

# --progress=plain


# --dns 8.8.8.8 --dns 8.8.4.4 